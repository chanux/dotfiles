[user]
	name = chanux
	email = chanuka@thinkcube.com

[alias]
  co = checkout
  ci = commit
  st = status
  br = branch
  lg = log --all --color --graph --date=short --abbrev-commit --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %C(cyan)%ad%Creset %Cgreen(%cr)%C(bold blue)<%an>%Creset'
  type = cat-file -t
  dump = cat-file -p

[core]
	excludesfile = /home/chanux/dotfiles/gitignore
    # requires diff-so-fancy
    # npm install -g diff-so-fancy
	pager = diff-so-fancy | less --tabs=1,5 -R
	editor = vim

[push]
	default = simple

[credential "https://source.developers.google.com"]
    helper = gcloud.sh
